name: Cloud Run Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Not implemented yet
        run: echo "TODO: Implement Cloud Run Deploy workflow."
